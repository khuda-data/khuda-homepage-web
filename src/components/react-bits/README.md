# React Bits Components

[React Bits](https://reactbits.dev)에서 가져온 컴포넌트를 관리하는 폴더입니다.

## 사용 방법

```bash
npx shadcn@latest add [component-name] --registry @react-bits --cwd src/components/react-bits
```

```typescript
import { ComponentName } from "@/react-bits/component-name";
```
